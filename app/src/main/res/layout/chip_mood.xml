<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
            name="item"
            type="com.karrar.movieapp.ui.matchSelection.MatchSelectionUiState.Mood" />
        <variable
            name="listener"
            type="com.karrar.movieapp.ui.matchSelection.MatchSelectionInteractionListener" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:gravity="center_vertical"
        android:background='@{item.isSelected ? @drawable/shape_chip_enabled_radius12 : @drawable/shape_chip_disabled_radius12}'
        android:clickable="true"
        android:focusable="true"
        android:onClick="@{() -> listener.onMoodClicked(item)}">

        <LinearLayout
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginHorizontal="12dp"
            android:background="@drawable/shape_chip_icon_disabled_radius10"
            android:gravity="center">

            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@{item.icon}" />
        </LinearLayout>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/manrope_regular"
            android:text="@{item.mood}"
            android:textColor="@color/shade_primary"
            android:textSize="14sp" />
    </LinearLayout>
</layout>