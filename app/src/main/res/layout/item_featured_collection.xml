<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/tools"
    app:ignore="NamespaceTypo">

    <data>

        <variable
            name="listener"
            type="com.karrar.movieapp.ui.adapters.FeaturedCollectionsInteractionListener" />

        <variable
            name="item"
            type="com.karrar.movieapp.ui.adapters.FeaturedCollectionUi" />
    </data>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/spacing_medium"
        android:layout_marginBottom="@dimen/spacing_normal"
        android:gravity="center"
        android:onClick="@{v -> listener.onClickItem(item.type)}"
        android:orientation="vertical">

        <FrameLayout
            android:layout_width="280dp"
            android:layout_height="80dp"
            android:background="@drawable/background_rounded_8">

            <com.google.android.material.imageview.ShapeableImageView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="@string/featured_collection_image"
                android:scaleType="centerCrop"
                android:src="@drawable/late_night_thrills"
                app:imageRes="@{item.type.imageRes}"
                app:shapeAppearanceOverlay="8dp" />

            <TextView
                style="@style/TextStyle.Body.sm.medium"
                android:layout_width="match_parent"
                android:layout_height="32dp"
                android:layout_gravity="bottom"
                android:background="@color/overlay_primary"
                android:gravity="center"
                android:text="@{item.type.titleRes}"
                android:textColor="@color/shade_primary"
                app:text="Late night thrills" />
        </FrameLayout>

        <View
            android:layout_width="match_parent"
            android:layout_height="6dp"
            android:layout_marginHorizontal="8dp"
            android:background="@drawable/background_rounded_8"
            android:backgroundTint="@color/brand_tertiary" />

        <View
            android:layout_width="match_parent"
            android:layout_height="6dp"
            android:layout_marginHorizontal="16dp"
            android:background="@drawable/background_rounded_8"
            android:backgroundTint="@color/shade_quinary" />

    </LinearLayout>
</layout>